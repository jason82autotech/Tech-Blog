<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Kdam+Thmor+Pro&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header class="p-5">
    <nav class="mr-3">
      <button class="teki">teki</button>
      <a href="/"><button class="btn btn-custom home-btn" id="homepage">Homepage</button></a>
      <a href="/dashboard"><button class="btn btn-custom dash-btn" id="dashboard">Dashboard</button></a>
      {{#if logged_in}}
      <button class="btn btn-custom logout-btn" id="logout">Logout</button>
      {{else}}
      <a href="/login"><button class="btn btn-custom login-btn" id="login">Login</button></a>
      {{/if}}
    </nav>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
